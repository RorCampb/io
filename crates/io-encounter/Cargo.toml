[package]
name = "io-encounter"
version = "0.1.0"
edition = "2021"

[dependencies]
io-game = { path = "../io-game" }
io-types = { path = "../io-types" }
io-world = { path = "../io-world" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
