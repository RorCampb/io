[package]
name = "io-assets"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
glam = "0.29.3"
io-types = { path = "../io-types" }
gltf = { version = "1.4", features = ["import"] }
